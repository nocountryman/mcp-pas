[project]
name = "pas-server"
version = "0.1.0"
requires-python = ">=3.12"
description = "MCP server for structured scientific reasoning"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pas"]
